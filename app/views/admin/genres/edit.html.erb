<div class="container">
  <h4><span class="py-2 px-4 bg-light">ジャンル編集</span></h4>
  <div class="row mt-5">
    <%= form_with model: [:admin, @genre] do |f| %>
      <%= render 'layouts/errors', obj: @genre %>
      <div>
        <span class="mr-5">ジャンル名</span>
        <%= f.text_field :name %>
        <%= f.submit "変更を保存", class: "ml-5 btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>
