<div class="container">
  <div class="row">
    <div class="col-2">
      <%= render 'genre_list', genres: @genres %>
    </div>

    <div class="col-10">
      <div class="jumbotron" style="margin-bottom: 20px; margin-right: 200px;">
          <h5 class="p-5" id="top-icon">
          ようこそ、ながのCAKEへ！</br>
          このサイトは、ケーキ販売のECサイトになります。</br>
          会員でない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。
        </h5>
      </div>
      <h3>新着商品</h3>
      <div class="d-flex">
          <% @items.first(4).each do |item| %>
            <table class="mr-4">
              <tbody>
                <tr>
                  <td><%= image_tag item.get_image(200,150) %></td>
                </tr>
                <tr>
                  <td><%= item.name %></br></td>
                </tr>
                <tr>
                  <td>¥<%= item.with_tax_price.to_s(:delimited) %></td>
                </tr>
              </tbody>
            </table>
          <% end %>
      </div>
      <div class="text-right">
        <%= link_to '全ての商品を見る>', items_path %>
      </div>
    </div>
  </div>
</div>